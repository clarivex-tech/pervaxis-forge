# pervaxis-forge-api

Backend for Pervaxis Forge — Engine + API.

## Projects
- `src/Pervaxis.Forge.Engine` — pure generation library (no external deps)
- `src/Pervaxis.Forge.Api` — ASP.NET Core Minimal API

## Day 1
Read `docs/FORGE_SOLUTION_STRUCTURE.md` and `docs/FORGE_BLUEPRINT_BFF.md` before writing any code.

## Run locally
```bash
dotnet restore
dotnet build
dotnet test
```
