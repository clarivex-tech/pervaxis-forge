# {{ model.manifest.service_name }}

Angular monolith scaffold for **{{ model.manifest.product }}**.

Generated by [Pervaxis Forge](https://github.com/clarivex-tech/pervaxis-forge).

---

## Prerequisites

- Node.js 20+
- npm 10+
- Angular CLI (`npm install -g @angular/cli`)
{{ if model.manifest.ui_targets | array.contains "mobile" }}
- Capacitor CLI (`npm install -g @capacitor/cli`)
- Android Studio (for Android builds)
- Xcode (for iOS builds, macOS only)
{{ end }}

## Getting Started

```bash
# Install dependencies
npm install

# Start development server
npm start

# Open browser at http://localhost:4200
```

## Available Scripts

| Command | Description |
|---------|-------------|
| `npm start` | Start development server |
| `npm run build` | Build for production |
| `npm test` | Run unit tests |
| `npm run lint` | Lint source files |
| `npm run build:prod` | Production build with optimizations |
{{ if model.manifest.ui_targets | array.contains "mobile" }}
| `npx cap add android` | Add Android platform |
| `npx cap add ios` | Add iOS platform |
| `npx cap sync` | Sync web assets to native platforms |
{{ end }}

## Project Structure

```
{{ model.manifest.service_name }}/
├── apps/
│   └── {{ model.manifest.service_name }}/
│       └── src/
│           ├── app/
│           │   ├── core/           # Guards, interceptors, services
│           │   ├── features/       # Feature modules (lazy-loaded)
│           │   ├── app.component.ts
│           │   ├── app.config.ts
│           │   └── app.routes.ts
│           ├── environments/       # Environment configs
│           ├── index.html
│           ├── main.ts
│           └── styles.scss
├── angular.json
├── package.json
└── tsconfig.json
```

## Architecture

- **Angular 21** standalone architecture (no NgModules)
- **Component prefix**: `{{ model.manifest.component_prefix }}`
- **Styling**: SCSS with CSS custom properties (design tokens)
- **Routing**: Lazy-loaded feature routes with auth guards
- **HTTP**: Interceptor-based auth and error handling
- **Error handling**: Global ErrorHandler with structured logging
{{ if model.manifest.ui_targets | array.contains "mobile" }}
- **Mobile**: Ionic + Capacitor for native deployment
- **Platform detection**: PlatformService for adaptive behavior
{{ end }}

## Vertical

- **Product**: {{ model.manifest.product }}
- **Vertical**: {{ model.manifest.vertical_slug }}

---

*Generated by Pervaxis Forge — Clarivex Technologies*
