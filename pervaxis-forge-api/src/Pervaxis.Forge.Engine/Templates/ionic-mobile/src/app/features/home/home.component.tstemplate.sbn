import { Component } from '@angular/core';
import { IonHeader, IonToolbar, IonTitle, IonContent } from '@ionic/angular/standalone';

/**
 * VERTICAL: This is your starting point.
 * Replace this placeholder with your domain-specific home screen.
 */
@Component({
  selector: '{{ model.manifest.component_prefix }}-home',
  standalone: true,
  imports: [IonHeader, IonToolbar, IonTitle, IonContent],
  template: `
    <ion-header>
      <ion-toolbar>
        <ion-title>{{ model.manifest.service_name }}</ion-title>
      </ion-toolbar>
    </ion-header>
    <ion-content class="ion-padding">
      <h1>Welcome to {{ model.manifest.service_name }}</h1>
      <p>This scaffold is ready for your business logic. Start building here.</p>
    </ion-content>
  `,
})
export class HomeComponent {}
