# {{ model.names.namespace }}

> Auto-generated by Pervaxis Forge. gRPC service.

## Endpoints

- gRPC: `https://localhost:7001`

## Genesis Modules

{{ for module in model.selected_modules -}}
- **{{ module.name }}** — `{{ module.package_name }}`
{{- end }}

## Getting Started

```bash
dotnet restore
dotnet build
dotnet run
```
