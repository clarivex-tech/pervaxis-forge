# {{ model.manifest.service_name }}

Angular Shell application for the **{{ model.manifest.vertical_slug }}** vertical — generated by Pervaxis Forge.

## Stack

- Angular 18 (standalone components)
- Nx workspace
- Pervaxis Canvas modules: {{ for m in model.selected_canvas_modules }}{{ m.name }}{{ if !for.last }}, {{ end }}{{ end }}

## Getting Started

```bash
npm install
npm start        # nx serve {{ model.manifest.service_name }}
npm test         # nx test {{ model.manifest.service_name }}
npm run build    # nx build {{ model.manifest.service_name }}
```

## Standards

Read `.claude/guides/` before making changes:
- `DOMAIN_CONVENTIONS.md` — naming, structure, and coding standards
- `GIT_WORKFLOW.md` — branching strategy and PR process
- `PRINT_DEVELOPER_GUIDE.md` — how to build domain modules
