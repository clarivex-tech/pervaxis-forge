<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>{{ model.names.namespace }}</RootNamespace>
    <AssemblyName>{{ model.names.namespace }}</AssemblyName>
    <!-- AnalysisMode, Nullable, TreatWarningsAsErrors, ImplicitUsings inherited from Directory.Build.props -->
  </PropertyGroup>

  <!-- [2.3.2] No Version attributes here — all versions pinned in Directory.Packages.props -->
  <ItemGroup>
    <PackageReference Include="HotChocolate.AspNetCore" />
    <PackageReference Include="HotChocolate.Data" />
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" />
    <PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Http" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" />
    <PackageReference Include="AWSSDK.SecretsManager" />
    <PackageReference Include="AWSSDK.Extensions.NETCore.Setup" />
{{- for module in model.selected_modules }}
    <PackageReference Include="{{ module.package_name }}" />
{{- end }}
{{- if model.manifest.database != null && model.manifest.database.engine == "postgresql" }}
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
{{- end }}
  </ItemGroup>

</Project>
